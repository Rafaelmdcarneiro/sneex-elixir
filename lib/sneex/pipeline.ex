defmodule Sneex.Pipeline do
  @moduledoc """
  This module handles the pipeline.

  The pipeline consists of:
  * Fetch (1 tick)
  * Decode (1 tick)
  * Execute (variable ticks)
  * Writeback (1 tick, optional)
  """
end
